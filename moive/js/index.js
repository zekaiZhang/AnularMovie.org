;(function (angular) {

    /*1.创建模板和总控制器
     *  1.angular.module('app')  不带[]  表示获取指定名称的模块。
     *  2.angular.module('app'，[]); 带[]   表示创建一个新的模块。
     * */
    var app = angular.module('app', ['ngRoute']);

})(angular);